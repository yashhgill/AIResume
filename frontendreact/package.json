{
  "name": "resume-generator-react",
  "version": "1.0.0",
  "description": "Resume Generator - React Frontend",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "install-deps": "npm install",
    "start": "vite",
    "start:frontend": "vite",
    "start:backend": "echo Backend runs on XAMPP Apache (port 80). Please ensure XAMPP is running.",
    "check:backend": "curl -s http://localhost/resume_generator/api/test_db.php || echo Backend not accessible"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "axios": "^1.6.2",
    "bootstrap": "^5.3.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8"
  }
}

